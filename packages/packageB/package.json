{
    "name": "package-b",
    "version": "1.0.0",
    "module": "dist/build.esm.js",
    "types": "dist/types",
    "files": [
        "dist"
    ],
    "scripts": {
        "build": "rimraf ./dist && rollup -c",
        "watch": "rollup -c -w",
        "preversion": "yarn build"
    }
}
